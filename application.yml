security:
  basic:
      enabled: false

management:
   security:
      enabled: false

logging:
   level:
       io:
           pivotal: DEBUG

spring:
  cloud:
      services:
            registrationMethod: route

  sleuth:
      sampler:
            percentage: 100.0
